import React from "react";
import { DataTable, type DataTableHandle } from "../../../../../shared/table/DataTable";
import type { ColumnDef, RowAction } from "../../../../../shared/table/types";
import { useAuth } from "../../../../../hooks/useAuth";
import JustificatifAbsenceService, { getJustificatifDisplayLabel, getJustificatifSecondaryLabel, getJustificatifStatusMeta, type JustificatifAbsenceWithRelations } from "../../../../../services/justificatifAbsence.service";

export default function JustificatifTable() { const { etablissement_id } = useAuth(); const tableRef = React.useRef<DataTableHandle>(null); const service = React.useMemo(() => new JustificatifAbsenceService(), []); const columns: ColumnDef<JustificatifAbsenceWithRelations>[] = [{ key: "eleve", header: "Eleve", render: (row) => <div><p className="font-medium text-slate-900">{getJustificatifDisplayLabel(row)}</p><p className="text-xs text-slate-500">{getJustificatifSecondaryLabel(row)}</p></div> }, { key: "statut", header: "Statut", render: (row) => { const meta = getJustificatifStatusMeta(row.statut); return <span className={`inline-flex rounded-full px-2.5 py-1 text-[11px] font-semibold ${meta.tone}`}>{meta.label}</span>; } }, { key: "document_url", header: "Document", render: (row) => row.document_url ?? "-" }]; const actions: RowAction<JustificatifAbsenceWithRelations>[] = [{ label: "Supprimer", variant: "danger", confirm: { title: "Suppression", message: "Supprimer ce justificatif ?" }, onClick: async (row) => { await service.delete(row.id); tableRef.current?.refresh(); } }]; return <DataTable<JustificatifAbsenceWithRelations> ref={tableRef} service={service} columns={columns} actions={actions} getRowId={(row) => row.id} initialQuery={{ page: 1, take: 10, where: etablissement_id ? { eleve: { etablissement_id } } : {}, includeSpec: { eleve: { include: { utilisateur: { include: { profil: true } } } }, motif: true }, orderBy: [{ date_debut: "desc" }] }} showSearch onSearchBuildWhere={(text) => ({ AND: [ ...(etablissement_id ? [{ eleve: { etablissement_id } }] : []), { OR: [{ eleve: { utilisateur: { profil: { prenom: { contains: text } } } } }, { eleve: { utilisateur: { profil: { nom: { contains: text } } } } }, { statut: { contains: text } }, { motif: { nom: { contains: text } } }] } ] })} />; }
